const token=localStorage.getItem('applicantToken');const $=id=>document.getElementById(id);const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));let existing=false;let workerCoords=null;
async function api(url,o={}){o.headers={...(o.headers||{}),Authorization:'Bearer '+token,'Content-Type':'application/json'};const r=await fetch(url,o);const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.message||'Request failed');return d;}
function setLocationStatus(message,error=false){$('locationStatus').textContent=message;$('locationStatus').className=error?'market-error':'market-muted';}
function captureLocation(){if(!navigator.geolocation)return setLocationStatus('Location is not supported. You can still use county and area.',true);setLocationStatus('Getting your location…');navigator.geolocation.getCurrentPosition(pos=>{workerCoords=[pos.coords.longitude,pos.coords.latitude];setLocationStatus('✓ Location captured automatically for nearby matching.')},err=>{const m={1:'Location permission was denied. Allow it in browser/site settings or continue manually.',2:'Location could not be determined. Turn on Location/GPS or continue manually.',3:'Location request timed out. Try again or continue manually.'};setLocationStatus(m[err.code]||'Unable to get your location. Continue manually.',true)},{enableHighAccuracy:false,timeout:6000,maximumAge:600000});}
async function init(){if(!token)throw Error('Please log in as an applicant first.');const catsResponse=await fetch('/api/marketplace/categories');const cats=await catsResponse.json().catch(()=>[]);if(!catsResponse.ok||!Array.isArray(cats))throw Error(cats.message||'Failed to load service categories.');$('services').innerHTML=cats.map(c=>`<option value="${esc(c._id)}">${esc(c.name)}</option>`).join('');try{const applicant=await api('/api/applicants/profile');
const applicantPhoto=String(applicant.profilePhoto||'');
$('photoPreview').innerHTML=applicantPhoto?`<img src="${esc(applicantPhoto)}" alt="Your Norvim profile photo" loading="eager" decoding="async">`:'<span>👤</span>';
const p=await api('/api/marketplace/worker-profile');existing=true;$('displayName').value=p.displayName||'';$('phone').value=p.applicantId?.phone||'';$('bio').value=p.bio||'';$('experience').value=p.experience||'';$('county').value=p.location?.county||'';$('area').value=p.location?.area||'';$('status').value=p.availability?.status||'Offline';$('radius').value=p.availability?.serviceRadiusKm||10;$('priceType').value=p.pricing?.type||'negotiable';$('amount').value=p.pricing?.amount??'';workerCoords=p.location?.coordinates?.coordinates||null;const ids=(p.services||[]).map(s=>s._id);[...$('services').options].forEach(o=>o.selected=ids.includes(o.value))}catch(e){if(!String(e.message).includes('not found'))$('message').innerHTML=`<p class="market-error">${esc(e.message)}</p>`}}
$('useLocation').addEventListener('click',captureLocation);$('form').addEventListener('submit',async e=>{e.preventDefault();try{const location={country:'Kenya',county:$('county').value,area:$('area').value};if(workerCoords)location.coordinates=workerCoords;const d=await api('/api/marketplace/worker-profile',{method:existing?'PUT':'POST',body:JSON.stringify({displayName:$('displayName').value,phone:$('phone').value.trim(),bio:$('bio').value,services:[...$('services').selectedOptions].map(o=>o.value),experience:$('experience').value,location,availability:{status:$('status').value,serviceRadiusKm:Number($('radius').value)},pricing:{type:$('priceType').value,amount:$('amount').value?Number($('amount').value):undefined}})});existing=true;$('message').innerHTML=`<p class="market-success">${esc(d.message)}</p>`}catch(e){$('message').innerHTML=`<p class="market-error">${esc(e.message)}</p>`}});init().catch(e=>$('message').innerHTML=`<p class="market-error">${esc(e.message)}</p>`);
