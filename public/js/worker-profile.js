const box=document.getElementById('content');const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
(async()=>{try{const id=new URLSearchParams(location.search).get('id');if(!id)throw Error('Worker not specified.');const r=await fetch('/api/marketplace/workers/'+encodeURIComponent(id));const w=await r.json();if(!r.ok)throw Error(w.message||'Worker not found.');const a=w.applicantId||{};box.innerHTML=`<article class="market-card"><h1>${esc(w.displayName||a.name)}</h1><p class="market-muted">${esc([w.location?.area,w.location?.county].filter(Boolean).join(', '))}</p><p>${esc(w.bio||'No bio provided.')}</p><h3>Services</h3><div class="market-tags">${(w.services||[]).map(s=>`<span class="market-tag">${esc(s.name)}</span>`).join('')}</div><h3>Skills</h3><p>${esc((w.skills||[]).join(', ')||'Not listed')}</p><h3>Experience</h3><p>${esc(w.experience||'Not listed')}</p><p><strong>Service radius:</strong> ${esc(w.availability?.serviceRadiusKm||10)} km</p><p><strong>Pricing:</strong> ${esc(w.pricing?.type||'negotiable')}${w.pricing?.amount!=null?' — KES '+esc(w.pricing.amount):''}</p><div class="market-actions"><a class="market-btn primary" href="marketplace-request.html?worker=${encodeURIComponent(w._id)}">Request this worker</a></div></article>`}catch(e){box.innerHTML=`<div class="market-error">${esc(e.message)}</div>`}})();
